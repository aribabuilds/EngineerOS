import type { PartialDictionary } from "./types";

/**
 * German namespace - STUB.
 *
 * Deliberately empty for now. The site ships English only (see build brief §0:
 * "Leave a `de` namespace stubbed"). When translations are ready, fill keys
 * here; anything omitted falls back to English via the deep-merge in index.ts.
 *
 * Example, once translated:
 *   hero: { h1: "Ich baue KI- und Automatisierungssysteme, …" }
 */
export const de: PartialDictionary = {
  // TODO: German translations to be added later.
};
